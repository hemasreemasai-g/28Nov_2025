function displayMessage(name) {
    console.log(`Hello, ${name}!`);
}
function getUserInput(callback) {
    setTimeout(() => {
        const username = "Hemasree";
        callback(username);  // calling the callback with retrieved name
    }, 1000);
}
getUserInput(displayMessage);
